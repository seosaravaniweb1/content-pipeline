# همه‌ی وابستگی‌ها رایگان‌اند. هیچ سرویس پولی در این برنامه استفاده نمی‌شود.

# --- هسته (بدون این‌ها اجرا نمی‌شود) ------------------------------------------
typer>=0.12
PyYAML>=6.0
# پنل مدیریت به هیچ بسته‌ی اضافه‌ای نیاز ندارد؛ سرور و صفحه‌اش فقط با
# کتابخانه‌ی استاندارد پایتون کار می‌کنند.

# --- گوگل‌شیت ----------------------------------------------------------------
gspread>=6.0              # برای نوشتن در گوگل‌شیت (بدون آن فقط فایل محلی)
openpyxl>=3.1             # خواندن/نوشتن xlsx

# --- شبکه و پارس -------------------------------------------------------------
curl_cffi>=0.7            # impersonate="chrome" — جایگزین requests خام
selectolax>=0.3           # پارس سریع HTML (اختیاری)
rapidfuzz>=3.9            # شباهت متنی (اختیاری ولی توصیه‌شده)
trafilatura>=1.8          # استخراج متن اصلی صفحه (اختیاری)

# --- اختیاری ------------------------------------------------------------------
Pillow>=10.0              # اثرانگشت تصویر: «همین کاور در سایت دیگر»
playwright>=1.45          # سایت‌های JS-heavy؛ پس از نصب: playwright install chromium

# --- تست ---------------------------------------------------------------------
pytest>=8.0
